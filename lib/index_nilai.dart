import 'package:flutter/material.dart';

class IndexNilai extends StatelessWidget {
  const IndexNilai({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
