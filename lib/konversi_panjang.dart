import 'package:flutter/material.dart';

class KonversiPanjang extends StatelessWidget {
  const KonversiPanjang({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
